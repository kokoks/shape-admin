Для работы проекта введите в терминал следующее:
mkdir -p config dbBackup backups
docker-compose up --build

Откройте в браузере:

http://localhost:8080

Важно: не открывайте HTML-файл напрямую через file://, используйте только http://localhost:8080.
